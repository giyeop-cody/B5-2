# B5-2 — 과제 의도 분석

## 1. 과제 의도
B5-1에서 SQL을 배운 후, FastAPI로 "요청 → 서버 → DB → 화면" 전체 흐름을 구현. 레이어 분리, PRG 패턴, ORM의 의미를 체득.

## 2. 기능의 의도
| 기능 | 의도 |
|------|------|
| 레이어 분리 (Router/Service/Repository) | "역할별로 코드를 나누는" 구조적 사고 |
| PRG 패턴 | "새로고침 중복 방지" = 웹의 기본 패턴 |
| SQLAlchemy ORM | B5-1의 SQL을 Python 코드로 → ORM이 해주는 일 |
| Jinja2 SSR | "서버에서 HTML 생성" = 화면 렌더링 방식 |
| CRUD 화면 흐름 | "등록→목록→상세→수정→삭제" 전체 사이클 |

## 3. 제약의 의도
| 제약 | 의도 |
|------|------|
| 지정 패키지만 사용 | "필요한 것만 쓰는" 최소 의존성 |
| 인증/관계 구현 안 함 | "이번 미션은 CRUD + 구조에 집중" (다음 미션에서 확장) |

## 4. 학습 목표의 의도
- "요청이 서버를 거쳐 DB까지 가는 흐름" → 전체 아키텍처 이해
- "GET vs POST" → HTTP 메서드의 역할
- "PRG" → 웹 패턴의 이해
- "레이어 분리" → 유지보수 가능한 구조

## 5. 평가 질문 의도
| 질문 방향 | 의도 |
|-----------|------|
| 라우터→서비스→저장소 흐름? | 전체 아키텍처 이해 |
| GET vs POST 차이? | HTTP 메서드 역할 |
| PRG 패턴 이유? | 웹 패턴 이해 |
| 레이어 분리 이유? | 유지보수 관점 |

## 6. 학습 방향
1. FastAPI 기본 (라우터, 요청/응답)
2. SQLAlchemy ORM (모델, 세션, CRUD)
3. Jinja2 템플릿 (SSR)
4. PRG 패턴
5. 레이어 분리 (Router → Service → Repository)

## 7. 이전/다음 과제와의 연결
- 이전: B5-1 (SQL 직접) — SQL을 ORM으로 추상화
- 다음: B5-3 (인증) — B5-2의 CRUD에 인증/인가/연관관계 추가
